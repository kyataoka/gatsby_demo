{
  "name": "gatsby_demo",
  "version": "1.0.0",
  "private": true,
  "description": "gatsby_demo",
  "author": "kataokahiroki",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "deploy": "git checkout master && git merge develop && git push origin master && gatsby build --prefix-paths && gh-pages -d public && git checkout develop"
  },
  "dependencies": {
    "@material-ui/core": "^4.11.3",
    "babel-plugin-styled-components": "^1.12.0",
    "gatsby": "^3.3.1",
    "gatsby-plugin-styled-components": "^4.3.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "styled-components": "^5.2.3"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
